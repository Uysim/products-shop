= simple_form_for([:admin, @category, @product]) do |f|
  = f.error_notification
  .form-inputs
    = f.input :name
    = f.input :price
    = f.association :category
    = f.input :description
  .form-actions
    = f.button :submit
