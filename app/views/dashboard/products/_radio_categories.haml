%ul
  - categories.each do |category|
    %li
      = f.radio_button :category_id, category.id
      = f.label category.name
    = render 'radio_categories', categories: category.subcategories, f: f if category.subcategories.any?
